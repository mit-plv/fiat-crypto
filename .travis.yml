language: generic

sudo: required
dist: trusty

env:
  matrix:
    - COQ_VERSION="8.5"  COQPRIME="coqprime"

before_install:
  - sudo add-apt-repository ppa:jgross-h/coq-backports -y
  - sudo apt-get update -q
  - sudo apt-get install coq -y

script: make COQPATH="$(pwd)/$COQPRIME" TIMED=1 -j2
